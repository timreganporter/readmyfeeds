<% title "Phones" %>

<table>
  <tr>
    <th>Number</th>
  </tr>
  <% for phone in @phones %>
    <tr>
      <td><%= phone.number %></td>
      <td><%= link_to "Show", phone %></td>
      <td><%= link_to "Edit", edit_phone_path(phone) %></td>
      <td><%= link_to "Delete", phone, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Phone", new_phone_path %></p>

<p><%= link_to "Back to Feed", feeds_path %></p>

